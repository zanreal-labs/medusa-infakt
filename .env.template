# Only needed to run `medusa plugin:db:generate` against a throwaway Postgres.
# See "Generating a migration" in the README for the full recipe, including the
# container name and port this repo standardises on. Never commit a filled-in copy.
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=55433
DB_NAME=medusa_infakt_dev
DATABASE_URL=postgres://postgres:postgres@localhost:55433/medusa_infakt_dev
