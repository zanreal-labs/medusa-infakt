# Only needed to run `medusa plugin:db:generate` against a throwaway Postgres.
# See "Generating a migration" in the README. Never commit a filled-in copy.
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=medusa_infakt_dev
